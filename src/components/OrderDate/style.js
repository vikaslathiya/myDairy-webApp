import {makeStyles} from "@material-ui/core/styles";

export const OrderDateStyle = makeStyles((theme) => ({
    typography: {
        '&.MuiTypography-root': {
            fontWeight: 600
        }

    }
}))